# edge

CPU-first edge detection service for the hybrid ReID pipeline.

## Baseline

The current demo/benchmark baseline is documented in:

- [/Users/macbook/Documents/reid-production/docs/edge-baseline-benchmark.md](/Users/macbook/Documents/reid-production/docs/edge-baseline-benchmark.md)

That note records:

- the current edge baseline config
- the benchmark command pattern
- tested model/runtime results
- which experiments were rejected and why
